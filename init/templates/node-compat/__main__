/** @typedef {import('pear-interface')} */ /* global Pear */
require('./compat') // keep at top
const { versions } = Pear
console.log('Pear terminal application running')
console.log(await versions())
