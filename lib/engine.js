const pipe = require('bare-pipe')
const { PIPE } = require('./constants.js')

// export default async function start (drive, store) {
//   console.log('starting various things...')
// }

module.exports = function start (drive) {
  console.log('i am the engine...')
}
